#import "./Note.graphql"

subscription onNoteUpdated($studentId: Int!) {
    noteUpdated(studentId: $studentId) {
        mutation
        id
        studentId
        node {
            ...NoteInfo
        }
    }
}
